name: hello-world
on: push
env:
  PATH: "zenTask/translation/test.yml"
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
       
        if: ${{env.PATH}} grep -q "translation" then
        run:
            ls
            pwd
           echo "Hello"
           
        
